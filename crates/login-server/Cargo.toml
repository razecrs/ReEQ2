[package]
name = "login-server"
version = "0.1.0"
edition.workspace = true
license.workspace = true
authors.workspace = true
description = "EQ2 login server (clean-room reimplementation) — milestone 1."

[dependencies]
protocol = { path = "../protocol" }

[lints]
workspace = true
