//! EQ2 login server — milestone 1.
//!
//! Goal: a real EQ2 client connects here, completes the handshake, and
//! receives a server list. Built bottom-up on the `protocol` crate.

fn main() {
    println!("eq2-rs login-server — not listening yet (scaffold).");
    // Next: bind the EQStream UDP socket and answer OP_GetLoginInfo.
}
