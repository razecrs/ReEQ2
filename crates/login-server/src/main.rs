//! eq2 login server — milestone 1.
//! goal: get a real eq2 client to connect here, run the handshake, and hand it
//! back the server list. building it up on the protocol crate.

fn main() {
    println!("ReEQ2 login-server — not listening yet, just scaffold for now");
    // next: bind the eqstream udp socket and answer OP_GetLoginInfo
}
